"use client";

import React from "react";

// ThemeToggle is a no-op in the current single-theme (dark navy) design.
// Kept as a named export so any existing imports don't break.
export const ThemeToggle: React.FC = () => {
  return null;
};
